import Foundation

/// Design tokens for opacity values used in component states.
///
/// These opacity values provide consistent visual feedback for interactive elements.
///
public extension Double {
  /// Opacity applied to components in the pressed state.
  /// Value: 0.7
  static let pressedOpacity = 0.7
}
